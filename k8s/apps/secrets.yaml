apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
type: Opaque
stringData:
  # Base64 encoding handled by kubectl apply if using stringData
  # In PROD, use ExternalSecrets or SealedSecrets
  database-url: "postgres://postgres:password@timescaledb.default.svc.cluster.local:5432/iot_db"
  mqtt-username: "backend_service"
  mqtt-password: "secure_password"
